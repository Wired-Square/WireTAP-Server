[package]
name = "wiretap-model"
description = "Shared WireTAP types: captured samples and the server's configuration schema"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

# Deliberately dependency-light: types only, no runtime, no drivers, no database.
# The web UI and any future CLI need the wire contract without the capture stack.
[dependencies]
serde.workspace = true
toml.workspace = true
